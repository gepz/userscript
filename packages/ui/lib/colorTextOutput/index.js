import { h, text, } from 'hyperapp';
export default (textStyle) => (color) => h('span', {
    style: {
        ...textStyle,
        color,
    },
}, text('Aa1あア亜'));
//# sourceMappingURL=index.js.map