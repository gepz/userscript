// The z-index every flowing .fyc_chat span carries (declared in
// @/mainCss). Anything that must paint above the chats inside the chat
// screen (they are siblings in the same stacking context) keys off this
// instead of restating the number.
export default 30;
