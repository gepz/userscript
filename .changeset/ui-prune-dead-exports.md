---
'@userscript/ui': minor
---

Pruned dead surface: the unused `setString`/`setStrings` setters and the
preact-based `node/checkboxRow2` are gone (dropping the `preact`,
`@effect/typeclass`, and `ts-toolbelt` dependencies).
