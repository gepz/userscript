---
'@userscript/eslint-config': patch
---

Require the `.ts`/`.mts` extension on relative imports in TS files outside
`src` (`tsWebpackConfig`), matching how Node resolves the webpack configs it
runs from source.
